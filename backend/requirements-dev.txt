# Dev & test dependencies — install alongside main:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx                              # TestClient transport for FastAPI
fakeredis[lua]>=2.26.0             # in-memory Redis for unit tests
ruff>=0.8.0                        # linter + formatter
mypy>=1.13.0                       # static type checking
