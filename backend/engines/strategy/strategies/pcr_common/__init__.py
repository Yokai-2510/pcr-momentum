"""pcr_common - shared library for the four pcr_analytics index strategies.

Library code only (indicators + tick engine); each strategy package is
independently configured, deployed and complete.
"""
